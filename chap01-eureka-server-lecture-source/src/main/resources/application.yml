# Eureka Server?? ????? ???? port?(?? ? ?? ?? ??)
server:
  port: 8761

# Eureka Server? ?? ??? Server?? ???
# Server? ???? ?? ?? register-with-eureka:false?? ??
# ?? ???? ????? ???? ?????? ?? fetch-registry: false? ??
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka